// Instruction - Eureka!!

/*
Take a number and sum its digits raised to the consecutive powers
*/

function sumDigPow(a, b) {
  let arr = [];
  let sum = 0;
  for (let i=1; i<b; i++) {
    sum+=Math(a,i);
    a++;
    if ()
  }
}